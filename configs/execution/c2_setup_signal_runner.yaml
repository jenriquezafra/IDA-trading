runner:
  mode: signal_only
  candidate_id: C2
  max_data_staleness_days: 3

strategy:
  strategy_id: c2_h9_googl_5min_opening_bias_followthrough_v1
  lifecycle_config_path: configs/setup_signal_portfolio_lifecycle_c2_googl_5min_monthly.yaml

data:
  features_path: null

paper:
  target_account: DU9782002
  target_symbol: GOOGL
  unit_size: 1
  order_type: MKT
  time_in_force: DAY
  execution_timing: next_bar_open_simulated
  send_orders: false

outputs:
  output_dir: results/paper/c2_signal_runner
